# This script includes all the functions to load in your data
# Loading packages
import pandas as pd
from datetime import datetime
from datetime import timedelta
import os
import numpy as np
import os

def loadAMF(path, measures=['TIMESTAMP_START','TA_F','SW_IN','VPD_F','P_F',
                            'NEE_VUT_REF','RECO_NT_VUT_REF','GPP_NT_VUT_REF']):
    
    # Thumb through each file in my AMFdata directory
    # Read in data
    # Select columns
    # Add a site column
    # Concatenate with eachother
    
    return AMF_data
    